require('source-map-support').install()

import {TextEncoder, TextDecoder} from 'util'
import {createDataViewPacketParser} from '../dist'

const pp = createDataViewPacketParser @:
  TextEncoder, TextDecoder

import demo_common from './_demo_common.jsy'
demo_common @ pp
